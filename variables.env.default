## Directions ##
# Copy this file to variables.env and replace the values with your own.

# NGINX
NGINX_HOST=localhost
NGINX_PORT=8080

# GoLang Application
APP_PORT=8080
APP_ENV=development

# PostgreSQL Database
POSTGRES_USER=postgres
POSTGRES_PASSWORD=password
POSTGRES_DB=mydb

# pgAdmin
PGADMIN_DEFAULT_EMAIL=admin@example.com
PGADMIN_DEFAULT_PASSWORD=adminpassword
PGADMIN_LISTEN_PORT=80
